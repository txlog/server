name: "txlog-server"
arch: "amd64"
platform: "linux"
version: "0.1"
release: "1"
epoch: "1"
section: "default"
priority: "extra"
maintainer: "Rodrigo de Avila <me@rda.run>"
description: |
  The Transaction Log project aims to track package history on RPM systems,
  compiling data on the number of updates and installs. Designed to enhance
  system reliability, this initiative collects and centralizes information,
  providing valuable insights into the evolution of packages.

  This package contains the Server, which receivez transactional data and stores
  it on a PostgreSQL database. It also provides an API to query the data.
vendor: "Rodrigo de Avila"
homepage: "https://github.com/txlog"
license: "MIT"
contents:
  - src: ./bin/txlog-server
    dst: /usr/bin/txlog-server

